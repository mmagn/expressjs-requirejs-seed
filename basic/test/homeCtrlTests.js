define(['should'],
function (should) {
  it('should should work', function(){
		should.exist(1);
	});

	describe('saveUrl', function(){
		it('should only allow to save a valid url', function(){



		});
	});

});